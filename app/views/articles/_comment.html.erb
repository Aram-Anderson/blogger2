<div>
  <h5>Comment by <%= comment.author_name %>, <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> after the original post</h5>
  <p class="comment"><%= comment.body %></p>
</div>
